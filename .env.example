# Server Configuration
PORT=3000

# Vercel Blob Configuration (for M3U8 playlist storage)
# 1. Deploy your project to Vercel
# 2. Go to Vercel dashboard → Your Project → Storage tab
# 3. Create a Blob store (if not already created)
# 4. Copy the BLOB_READ_WRITE_TOKEN from the dashboard
# 5. Set the variable below:

# Your Vercel Blob read/write token (get from Vercel dashboard)
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_1234567890abcdef

# Note: This token is automatically available in Vercel deployments
# You only need to set it manually for local development or other platforms
